name: oku
description: A simple PDF library and viewer.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  flutter_pdfview: ^1.3.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
